{
  "name": "typed-dynamodb",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter vitest-environment-dynalite build",
    "test": "pnpm run -r test"
  },
  "devDependencies": {
    "prettier": "^2.8.8"
  }
}
