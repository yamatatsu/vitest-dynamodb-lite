{
  "name": "vitest-dynamodb-lite-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "pnpm run -r --sequential test"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  },
  "engines": {
    "pnpm": "^10.x.x"
  }
}
