{
  "name": "vitest-dynamodb-lite-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test": "pnpm run -r --sequential test"
  },
  "devDependencies": {
    "js-green-licenses": "^4.0.0",
    "prettier": "^3.8.1"
  },
  "engines": {
    "pnpm": "^10.x.x"
  }
}
